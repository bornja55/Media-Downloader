## Agent skills

### Issue tracker

Issues and PRDs for this repo live as markdown files in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage roles are mostly defaults, except `needs-triage` is mapped to `need-triage`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo structure (one CONTEXT.md and docs/adr/ at the root). See `docs/agents/domain.md`.
